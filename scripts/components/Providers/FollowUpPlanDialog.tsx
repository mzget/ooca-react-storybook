import * as React from 'react';

import { PrimaryDialogButton } from '../../StyleComponents/DialogContentStyles';

export class FollowUpPlanDialog extends React.Component {
    render() {
        return (
            <div>
                <PrimaryDialogButton provider={true} onClick={() => { }} />
            </div>
        );
    }
}